import {
  FlatList,
  ScrollView,
  TouchableOpacity,
  TouchableWithoutFeedback,
  View,
} from 'react-native';

export {
  TouchableOpacity,
  TouchableWithoutFeedback,
  ScrollView,
  FlatList,
  View as GestureHandlerProvider,
};
